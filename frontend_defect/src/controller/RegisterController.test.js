import RegisterController from "./RegisterController.js";

describe("RegisterController", () => {

    it("is invalid for an empty input", () => {
        
        //const { values } = RegisterController();
        //expect(values.isValid).toBe(false);
        
    })
});