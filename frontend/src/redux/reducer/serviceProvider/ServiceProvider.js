const initialState = {
    byId: {},
    allIds: []
};

const ServiceProvider = (state = initialState, action) => {
    switch (action?.type) {
        default:
            return state;
    }
}

export default ServiceProvider;