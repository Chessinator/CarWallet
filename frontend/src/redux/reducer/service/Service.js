const initialState = {
    byId: {},
    byServiceProviderId: {},
    byVehicleId: {},
    allIds: []
}

const Service = (state = initialState, action) => {
    switch (action?.type) {
        default:
            return state;
    }
}

export default Service;